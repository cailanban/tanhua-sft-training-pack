# 对方拉取与还原说明（训练集采样版）

本仓库 = 三合一 SFT 训练集采样版（36,007 train + 3,715 valid）+ QLoRA 配置，可直接开训 27B 级 Qwen。
仓库为**私有**，需先获得访问权限（见第 0 步）。

## 0. 获取访问
- 让仓库主把你加为 collaborator（Settings → Collaborators），或
- 仓库主把仓库改为 public，或
- 用仓库主提供的 token（不推荐，注意保密）

## 1. 拉取
```bash
git clone https://github.com/cailanban/tanhua-sft-training-pack.git
cd tanhua-sft-training-pack
```
（如需 token：`git clone https://<USER>:<TOKEN>@github.com/cailanban/tanhua-sft-training-pack.git`）

## 2. 还原训练集（分片 -> 单文件）
```bash
bash reassemble.sh        # cat train_part_* > train_sampled_alpaca.jsonl
```

## 3. 校验完整性（必须做）
还原后核对 md5 与行数，任一不符说明传输损坏，重新拉取：
```bash
md5sum train_sampled_alpaca.jsonl   # 应为 982dc632545d8fdfa5df01a761b60580
md5sum valid_alpaca.jsonl           # 应为 357dfeed05616dfc842236bd4402b77c
wc -l train_sampled_alpaca.jsonl    # 应为 36007
wc -l valid_alpaca.jsonl            # 应为 3715
```
（macOS 用 `md5` 代替 `md5sum`）

## 4. 开始训练
模型用 `Qwen/Qwen2.5-32B-Instruct-GPTQ-Int4`（或你的 27B 目标模型），改 yaml 里 `model_name_or_path`。
```bash
# 80G 卡（A100/A800/H800）跑 32K：
llamafactory-cli train qlora_27b_a100.yaml
# 32G 卡（5090/4080S）跑 16K：
llamafactory-cli train qlora_27b_5090.yaml
```
- `dataset_info.json` 已注册 train/valid，LLaMA-Factory 直接识别
- 截断率/显存/超参/踩坑见 `README_训练建议.md`、`analyze_lengths.py`、`sampling_config.json`

## 文件清单
| 文件 | 说明 |
|---|---|
| train_part_aa..ah | 训练集分片（cat 还原） |
| valid_alpaca.jsonl | 验证集（完整） |
| reassemble.sh | 还原脚本 |
| dataset_info.json | LLaMA-Factory 注册 |
| qlora_27b_a100.yaml | 32K 配置（80G） |
| qlora_27b_5090.yaml | 16K 配置（32G） |
| sampling_config.json | 采样权重+seed |
| analyze_lengths.py | 截断率分析 |
| README_训练建议.md | 硬件/超参/踩坑全记录 |

完整未采样原始数据（40,428 条，1.8G，含 4 个 pool）不在本仓库，如需向仓库主索取。
